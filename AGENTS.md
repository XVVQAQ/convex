# Project Instructions

This project uses:

- Rust 2024 Edition
- Slint UI

## Slint

- Always consult the Slint documentation before writing or modifying `.slint` files.
- Validate `.slint` files with `slint-viewer.exe` instead of using `cargo check`.
- Do not guess Slint syntax or component APIs.
